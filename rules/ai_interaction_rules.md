# AI 交互规则

## 1. Git Ignore 文件读取规则

**触发标签**：`【强制读取】` (或 `[FORCE_READ]`)

**规则**：
当用户使用上述标签，或明确要求读取被 `.gitignore` 排除的文件时，AI 必须：
1. **禁止**使用 `view_file` 等标准工具。
2. **必须**使用终端命令 (`Get-Content` 或 `cat`) 读取文件。
